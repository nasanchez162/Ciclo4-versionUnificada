define(['controller/_itemController','delegate/itemDelegate'], function() {
    App.Controller.ItemController = App.Controller._ItemController.extend({

    });
    return App.Controller.ItemController;
}); 