define(['controller/_documentoController','delegate/documentoDelegate'], function() {
    App.Controller.DocumentoController = App.Controller._DocumentoController.extend({

    });
    return App.Controller.DocumentoController;
}); 